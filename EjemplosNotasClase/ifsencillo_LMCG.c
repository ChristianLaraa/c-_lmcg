/*Este programa nos servira para saber como se utiliza la instruccion simple if
*/
#include <stdio.h>
int a.b;

int main (void){
printf("\n Introduce el valor de a: ");
scanf("%i", &a);

printf("\n Introduce el valor de b: ");
scanf("%i", &b);

/*Declaración sencilla de if
Sintaxis if(Condición por operadores lógicos o booleanos)
*/

if (a%b == 0 ){
printf("\n %i es divisible por %d \n", a,b);
}

printf ("\n %i es divisible por %d");

return 0;
}


}

